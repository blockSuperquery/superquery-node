# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Environment
.env
.env.local

# Editors
.idea/
.vscode/
*.swp
.history/

# OS
.DS_Store

# Claude Code local state.
#
# The architecture reference and the task plan under .claude/docs and
# .claude/tasks ARE tracked — they are the project's design record and every
# contributor needs them. Everything else under .claude is per-developer.
.claude/*
!.claude/docs/
!.claude/tasks/
